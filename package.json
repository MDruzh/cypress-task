{
  "name": "cypress-task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:api": "npx cypress run --spec cypress/e2e/APITests.spec.cy.js",
    "cy:ui": "npx cypress run --spec cypress/e2e/UITests.spec.cy.js",
    "cy:all": "npx cypress run --spec cypress/e2e/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^14.2.0"
  }
}
